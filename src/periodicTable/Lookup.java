package periodicTable;

public class Lookup {

    /**
     * This method returns the atomic number of a chemical element by provided symbol.
     * Returns 0 if not found.
     */
    public static int numberBySymbol(String symbol){
        int number = 0;
        return number;
    }

    /**
     * This method returns the full name of a chemical element by its atomic number.
     * Returns 0 if not found.
     */
    public static String elementName(int number){
        return "not finished";
    }

    /**
     * This method returns the total amount of defined elements.
     */
    private static int totalElementCount(){
        return 0;
    }

    /**
     * This method reads database entries.
     * Returns contents of a file line by line as an array of strings.
     */
    private static String[] fileRead(String filePath){
        //read filePath
        return new String[]{"empty string"};
    }

}
